#include<stdio.h>
int main()
{
 int input;
 print("Enter the input\n");
 scanf("%c",&input);
 if(iput >='A' && input >='z') || (input >= 'a' && input <= 'z'))
 {
  print("Alphabet");
 }
  else
  {
   printf("no");
  }
  return 0;
 }
   
